{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "new-instance*", "instance-*"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:default",
    "dialog:default"
  ]
}
